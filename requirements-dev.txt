# Faqat testlar uchun (productionда kerak emas):
#   pip install -r requirements-dev.txt
#   pytest -q
-r requirements.txt
pytest>=8.0
httpx>=0.27
html5lib>=1.1        # brauzer qoidalari bo'yicha HTML tahlili (forma ichida forma testi)
